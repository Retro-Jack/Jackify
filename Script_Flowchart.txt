  ┌─────────────────────────────┐
  │            START            │
  └──────────────┬──────────────┘
                 │
                 ▼
  ┌─────────────────────────────┐
  │       Parse Arguments       │
  └──────────────┬──────────────┘
                 │
              --help? ──yes──► Print help & exit
                 │no
                 │
            preset set? ──no──► die: no preset
                 │yes
                 │
                 ▼
  ┌─────────────────────────────┐
  │     Check prerequisites     │
  │  downloads, HB CLI, preset  │
  └──────────────┬──────────────┘
                 │
                 │ fail ──► die
                 │ pass
                 │
        videos in downloads?
          │yes            │no
          │               │
          ▼               │
    ┌───────────┐    (use staging
    │  STEP 1   │       as-is)
    │  Copy to  │         │
    │  staging  │         │
    │  (videos  │         │
    │& subtitles│         │
    └─────┬─────┘         │
          │               │
          └──────┬────────┘
                 │
       videos in staging? ──no──► die: nothing to do
                 │yes
                 │
                 ▼
  ┌─────────────────────────────┐
  │          STEP 2             │
  │   For each video:           │
  │   • show progress bar       │
  │   • skip if already done    │
  │   • only file in dir?       │
  │     yes → flat to output    │
  │     no  → preserve subdir   │
  │   • copy matching subtitles │
  └──────────────┬──────────────┘
                 │
                 ▼
  ┌─────────────────────────────┐
  │          STEP 3             │
  │  Clean up names (media only)│
  │  • strip title numbers      │
  │  • strip source tags        │
  │  • separators → spaces      │
  │  • collapse spaces          │
  │  • title case               │
  └──────────────┬──────────────┘
                 │
                 ▼
  ┌─────────────────────────────┐
  │        Final Report         │
  │  (errors → error_log.txt)   │
  └──────────────┬──────────────┘
                 │
       clean staging? ──no──► DONE
                 │yes
                 ▼
  ┌─────────────────────────────┐
  │    Clear staging folder     │
  └──────────────┬──────────────┘
                 │
                 ▼
               DONE
